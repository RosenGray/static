window.process = { env: { NODE_ENV: "production" } };
let rootNode = document.getElementById("root");
function loadScript(e) {
  return new Promise((o, t) => {
    const n = document.createElement("script");
    (n.src = e),
      (n.crossOrigin = "anonymous"),
      (n.onload = o),
      (n.onerror = t),
      document.head.appendChild(n);
  });
}
function loadStyle(e) {
  return new Promise((o, t) => {
    const n = document.createElement("link");
    (n.rel = "stylesheet"),
      (n.href = e),
      (n.onload = o),
      (n.onerror = t),
      document.head.appendChild(n);
  });
}
const reactCdn =
    "development" === window.process.env.NODE_ENV
      ? "https://unpkg.com/react@18/umd/react.development.js"
      : "https://unpkg.com/react@18/umd/react.production.min.js",
  reactDomCdn =
    "development" === window.process.env.NODE_ENV
      ? "https://unpkg.com/react-dom@18/umd/react-dom.development.js"
      : "https://unpkg.com/react-dom@18/umd/react-dom.production.min.js";
if (
  (loadScript(reactCdn)
    .then(() => loadScript(reactDomCdn))
    .then(() => loadStyle("./accessibilik/style.css"))
    .then(() => loadScript("./accessibilik/accessibilik.umd.min.cjs"))
    .catch((e) => {
      console.error("Failed to load React or ReactDOM", e);
    }),
  !rootNode)
) {
  ((rootNode = document.createElement("div")).id = "root-accessibilik"),
    document.getElementsByTagName("body")[0].append(rootNode);
}
